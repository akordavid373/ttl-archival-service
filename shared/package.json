{
  "name": "@ttl-archival/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for TTL-Aware Automated Archival Service",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "files": [
    "dist/**/*"
  ]
}
